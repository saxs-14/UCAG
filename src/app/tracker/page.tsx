import { ApplicationTrackerShell } from '@/components/tracker/ApplicationTrackerShell';

export const metadata = { title: 'Application Tracker — UCAG' };

export default function TrackerPage() {
  return <ApplicationTrackerShell />;
}
